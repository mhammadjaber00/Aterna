<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application
            android:name=".AternaApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
                android:exported="true"
                android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
                android:name="io.yavero.aterna.feature.quest.notification.QuestActionReceiver"
                android:exported="false">
            <intent-filter>
                <action android:name="io.yavero.aterna.QUEST_PAUSE"/>
                <action android:name="io.yavero.aterna.QUEST_RESUME"/>
                <action android:name="io.yavero.aterna.QUEST_CANCEL"/>
                <action android:name="io.yavero.aterna.QUEST_COMPLETE"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>